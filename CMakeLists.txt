cmake_minimum_required(VERSION 3.24.0)

project(NDN)

# define targets
# include directories
# create targets

# simple ndn executable

include_directories("./src/")
add_library()
add_executable(main_sample ./scratch/scratch-simulator.cc)